PHASE 9 → PHASE 10 HANDOFF PROMPT
============================

Context (authoritative)
- Phase 7 remains the single execution authority (arming ceremony, receipts, traces, idempotency-first).
- Phase 8 adapter boundary guarantees remain enforced by tests (network default-deny, redaction, fail-closed).
- Phase 9 introduced read-only live HTTP snapshot scaffolding only:
  - deny-first allowlist: (adapter_id, capability, domain), no wildcards
  - NetworkGate http_get_bytes: timeout + max_bytes + no retries + allowlist enforcement
  - minimal Coinbase public snapshot adapter (no creds), strict schema checks
  - Phase 8 contract compatibility preserved (legacy get_market_snapshot wrapper + dummy_toxic redaction test path)

Non-negotiables (carry forward)
- NO execution/trading, NO signing/account endpoints, NO withdrawals/transfers.
- NO websockets, NO background daemons, NO hidden retries.
- NO secret leakage; redaction on all outward paths.
- Network egress must remain allowlist-driven and fail-closed.

Phase 10 scope suggestion (still read-only unless explicitly re-scoped)
- Expand read-only market data coverage while preserving invariants:
  - Add 1–2 additional public snapshot sources (separate adapters) with tests.
  - Normalize snapshot schema (price, ts, source, pair) into a small internal struct.
  - Add explicit failure classification tokens for:
    DENY_NETWORK_DISABLED, DENY_NOT_ALLOWLISTED_DOMAIN, DENY_RESPONSE_TOO_LARGE, DENY_BAD_JSON, DENY_SCHEMA_*
  - Ensure AdapterBroker.call remains the canonical route for live reads.

Wrap procedure (mandatory)
- Any Phase 10 wrap artifacts must be created via python writer-pattern and committed/pushed.
