PHASE 13 — BUILD NOTES
============================================================
As-of: 2026-01-05 08:08:33 UTC
Repo: /home/allmight/Allmight
Branch: main
HEAD: 0bdf7eb011090a7999f6af1d1a9655a30f0b6863

PHASE 13 INTENT
------------------------------------------------------------
Hardening + Operationalization only:
- Shadow A/B artifact guarantees
- Repo scan-safety enforcement
- Deterministic outputs (where feasible)
- Documentation alignment via enforced reality
NO feature expansion.

WHAT CHANGED (OPERATOR-RELEVANT)
------------------------------------------------------------
1) Repo Scan Safety Guardrail
- Added/maintained a test that forbids raw traversal primitives:
  - Path.' + 'rglob(...)
  - .' + 'rglob(...)
  - os.walk(...)
- Exception: os.walk may exist ONLY in scripts/tools/repo_files.py (hardened fallback).
- Guardrail test was hardened to avoid self-matching by building forbidden tokens without embedding exact substrings.

2) Shadow A/B Artifact Guarantees
- Enforced anomalies.log presence for every run (zero-anomaly runs included).
- Added exception-path regression test ensuring anomalies.log exists even when the run raises after run_dir creation.

3) Shadow A/B Determinism Enforcement
- Added determinism test asserting byte-for-byte stability for key artifacts across identical inputs.
- Normalized metrics_detail.json timing fields (avg_ms/max_ms) within the determinism test because latency is inherently nondeterministic.

VALIDATION EVIDENCE
------------------------------------------------------------
pytest -q:
........................................................................ [ 62%]
............................................                             [100%]
116 passed in 4.90s

git status --porcelain (should be empty at wrap time):
(clean)

Recent commits (git log -n 10):
0bdf7eb phase13: normalize shadow_ab determinism test (ignore timing fields)
17fd137 phase13: add exception-path test for shadow_ab anomalies.log guarantee
bfa243e phase13: fix scan-safety guardrail self-match + enforce shadow_ab anomalies.log
c324c30 shadow-ab: key-aligned merge + phase2 regime surfacing + ignore artifacts
ba1f2fd hygiene: ignore shadow artifacts and local snapshot fixtures
3a5ffb5 phase13: enforce scan-safety (no rglob/os.walk) + align docs
8ab341f phase12: add Phase 13 handoff prompt
f5d911b docs: update repo search appendices to recommend safe enumerator
bda059f tools: harden schema inventory scan (avoid filesystem walk hazards)
91e89c1 tools: harden phase wip scan (tracked-first repo enumeration)

FILES ADDED / MODIFIED (PHASE 13)
------------------------------------------------------------
- tests/phase13/test_repo_scan_safety.py (guardrail; self-match-safe)
- scripts/shadow_ab/runner.py (anomalies.log guarantee restored)
- tests/shadow_ab/test_shadow_ab_anomalies_log_guarantee.py (exception-path invariant)
- tests/shadow_ab/test_shadow_ab_deterministic_outputs.py (determinism invariant; normalized timing fields)

NOTES / NON-GOALS
------------------------------------------------------------
- No new data sources
- No new models
- No new execution logic
- Shadow evaluation only; execution remains DISABLED

END PHASE 13 BUILD NOTES
============================================================
