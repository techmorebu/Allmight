BUG-003: AvgVol_20 N/A and VolSpikeFlag not populating
Common causes:
- <20 valid volume rows for AssetID+Timeframe
- Volume column contains timestamps (wrong kline field)
- AssetID/Timeframe mismatches (spaces, different casing)

Fix:
- Verify Data_Staging schema: A AssetID, B Timeframe, ... H Volume
- Use SEQUENCE-based window formula with IFERROR
- Ensure Volume cells are numeric (Format -> Number)
- Confirm exact matches: "BTC" and "15m" with no trailing spaces
