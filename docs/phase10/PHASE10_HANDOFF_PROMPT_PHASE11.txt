PHASE 11 HANDOFF PROMPT
=======================
As-of: 2026-01-02 01:19 UTC
Status: ACTIVE (Phase 11 begins when Phase 10 is frozen)
Predecessor: Phase 10 (Kraken read-only adapter + MarketSnapshot normalization)

0. NON-NEGOTIABLE ENTRY PROCEDURE
--------------------------------
Before any Phase 11 work begins, the following MUST be completed:

1) Read:
   - docs/appendices/APPX_CHAT_RESPONSE_PROTOCOL.txt
   - docs/appendices/APPX_PROJECT_RAPID_SCAN_PROTOCOL.txt

2) Run:
   python tools/rapid_scan.py

If this step is skipped, all subsequent work is invalid.

1. RAPID ORIENTATION (REQUIRED)
-------------------------------
Current Phase: 11

Phase 10 delivered (authoritative):
- Added Kraken public HTTP snapshot adapter:
  - adapter_id: phase10_http_snapshot_kraken
  - allowlist domain: api.kraken.com
- Extended AdapterBroker market_snapshot_live routing to explicitly support:
  - phase9_http_snapshot (Coinbase)
  - phase10_http_snapshot_kraken (Kraken)
- Introduced shared snapshot struct:
  - allmight/adapters/market_snapshot.py (MarketSnapshot dataclass)
- Test suite remains green (83 passing).

Entry points (read in order):
- docs/build_notes/PHASE10_BUILD_NOTES.txt
- docs/bug_notes/PHASE10_BUG_NOTES.txt
- docs/phase10/PHASE10_OPERATOR_NOTES.txt
- docs/phase9/PHASE9_HANDOFF_PROMPT_PHASE10.txt (for invariant inheritance)

2. PHASE 11 SCOPE (INITIAL)
---------------------------
Phase 11 is concerned with read-only consumption/aggregation of snapshots WITHOUT expanding trust, such as:
- Optional: a small internal “snapshot normalization/merge” layer for multi-source reads
- Optional: source reconciliation rules (e.g., pick-first, median, reject outliers) WITH tests
- Optional: stricter shared validation helpers (pure refactor) without changing behavior

Out-of-scope unless explicitly re-scoped:
- Any trading/execution/signing
- Any account endpoints
- Any credential handling
- Websockets/daemons/schedulers
- Retries/backoff

3. HARD INVARIANTS (CANNOT BE BROKEN)
------------------------------------
- Network is default-deny.
- All live operations route through AdapterBroker.call.
- Allowlists are explicit and capability-scoped (no wildcards).
- Tests define behavior, not comments.
- Redaction is never bypassed for real adapters.
- No retries, no credentials, no write operations.

4. REQUIRED OUTPUTS FOR PHASE 11
--------------------------------
Phase 11 MUST conclude with:
- Build notes
- Bug notes
- Operator notes
- Phase 12 handoff
- Updated appendices only if behavior changes

5. META
-------
If a future operator asks:
"What am I allowed to do?"

Then this handoff or the appendices have failed.

END OF PHASE 11 HANDOFF
