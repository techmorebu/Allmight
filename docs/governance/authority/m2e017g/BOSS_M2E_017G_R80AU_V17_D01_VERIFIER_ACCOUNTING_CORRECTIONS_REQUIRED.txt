BOSS M2E-017G R80AU — V17 STATIC REVIEW / D01 VERIFIER + SLICE ACCOUNTING CORRECTION REQUIRED

TARGET RUNNER REVIEWED

  r80_runner_v17.sh

Exact delivered-file measurements independently verified:

  SHA256     530f40d529f5f6f18ae8a05c18a40f5e7f33080bb7d79d5b87dac99325f46134
  BYTES      76719
  wc -l      1279
  bash -n    PASS

Target executor remains:

  583cfdbc4dff6a3351136dc98257f19ed1c2735348c6eb553c2ee8e66d50eae7

======================================================================
R80AT CORRECTIONS — ACCEPTED IN SUBSTANCE
======================================================================

The following v17 corrections are materially present:

1. P5 final verdict gates D01 admission.

   For both real fixture PIDs:
     six ownership predicates are checked;
     OW_VERDICT is preserved;
     OW_REASON is preserved;
     OW_VERDICT must equal PASS.

2. D01 closure is no longer unconditionally hard-coded FROZEN.

   v17 derives D01_V_CL from fixture prerequisites.

3. Cleanup lineage is materially tightened.

   Root PPID is captured at creation.
   Descendant requires exact current PPID == root.
   Root requires exact current PPID == captured root PPID.
   P5 is rerun immediately before TERM.
   Starttime and cmdline are revalidated.

4. Exact-kill probe now expands and gates.

   RK_EXACT uses expanded CMDPOS.
   A known-positive control is present.
   RK_EXACT != 1 increments SAFETY_FAIL.

These are accepted.

v17 is nevertheless NOT host-run-authorized because two verifier/accounting
defects remain.

======================================================================
BLOCKER 1 — D01 INDEPENDENT VERIFIER DOES NOT PROVE STARTTIME IDENTITY
======================================================================

Exact G7 source reads descendant identity through PROC_ROOT:

  present(child)
  cmdline_of(child)
  starttime_of(child)

The independent D01 verifier currently checks:

  root is in map
  root synthetic stat file exists
  descendant synthetic stat file exists
  real parentage matches
  synthetic descendant cmdline matches runner-local regex

but it does NOT parse the synthetic descendant stat and prove:

  synthetic descendant starttime == captured real descendant starttime

Existence of the stat file is not identity equivalence.

This matters because D01 is certifying a frozen descendant identity, not merely
the existence of a mirror path.

REQUIRED CORRECTION:

From fixture evidence only, before using FROZEN as verifier_actual:

  read field 22/starttime from:
    $A_WORLD/proc/$D01_RD/stat

  compare it to:
    D01_DST

Require exact equality.

Also verify the root mirror starttime if the exact G7 path depends on a
LIVE_VERIFIED baseline root before descendant discovery.

Required labels:

  D01_VERIFIER_DESC_SYNTH_STARTTIME=<value>
  D01_VERIFIER_DESC_CAPTURED_STARTTIME=<value>
  D01_VERIFIER_DESC_STARTTIME_MATCH=YES

and, if applicable:

  D01_VERIFIER_ROOT_SYNTH_STARTTIME=<value>
  D01_VERIFIER_ROOT_CAPTURED_STARTTIME=<value>
  D01_VERIFIER_ROOT_STARTTIME_MATCH=YES

D01_V_CL may equal FROZEN only if all exact fixture-side identity prerequisites
are proven.

If any are absent/mismatched:

  D01_V_CL=CLOSURE_PREREQUISITES_NOT_CONSTRUCTED

======================================================================
BLOCKER 2 — DESC_SIGS PREDICATE IS NOT MECHANICALLY BOUND TO EXECUTOR SOURCE
======================================================================

v17 uses:

  grep -qE '^sleep [0-9]+$' synthetic-descendant-cmdline

and labels that result as a DESC_SIGS match.

The exact-source requirement from R80AT was stronger:

  D01_VERIFIER_DESC_SIG_EXACT_SOURCE_PREDICATE=YES

The runner currently does not prove that:

  '^sleep [0-9]+$'

is the exact predicate used by executor 583cfdbc... for descendant signatures.

A runner-local regex that happens to resemble the expected source is not enough.

REQUIRED CORRECTION:

Read or mechanically bind the exact DESC_SIGS definition from the exact
executor bytes.

Preferred:

  derive the source predicate from PATH_EXEC;
  validate that the expected exact definition occurs once;
  apply that exact source-grounded predicate to the synthetic descendant
  cmdline.

Alternatively, use a fixed authority constant ONLY if its exact text is
first verified against PATH_EXEC and the executor SHA is already bound.

Required evidence:

  D01_EXECUTOR_DESC_SIG_DEFINITION=<exact source-grounded definition>
  D01_EXECUTOR_DESC_SIG_DEFINITION_MATCH_COUNT=1
  D01_VERIFIER_DESC_SIG_EXACT_SOURCE_PREDICATE=YES
  D01_VERIFIER_DESC_SIG_MATCH=YES

If the exact source definition cannot be resolved uniquely:

  BLOCK D01
  do not guess.

======================================================================
BLOCKER 3 — PER-SLICE BLOCKED ACCOUNTING IS WRONG
======================================================================

The runner uses global accumulators:

  CASES_BLOCKED_REACH
  CASES_BLOCKED_CONTRACT

for both I and D cases.

At finalization v17 emits:

  I_CASES_BLOCKED_REACHABILITY="$CASES_BLOCKED_REACH"
  I_CASES_BLOCKED_CERTIFICATION_CONTRACT="$CASES_BLOCKED_CONTRACT"

after D02-D06 have already been recorded.

Therefore the fields labeled I_CASES_* include D blocked cases.

That is incorrect.

Separately, v17 hard-codes:

  D_CASES_BLOCKED_REACHABILITY=3

but current D disposition is:

  D02 BLOCKED_REACHABILITY
  D03 BLOCKED_REACHABILITY
  D04 BLOCKED_REACHABILITY
  D06 BLOCKED_REACHABILITY

which is FOUR.

Correct slice state:

  I:
    blocked reachability = 2  (I04,I05)
    blocked contract     = 1  (I06)
    blocked total        = 3

  D:
    blocked reachability = 4  (D02,D03,D04,D06)
    blocked contract     = 1  (D05)
    blocked total        = 5

  GLOBAL:
    blocked reachability = 6
    blocked contract     = 2
    blocked total        = 8

REQUIRED CORRECTION:

Use distinct per-slice counters or deterministic case-ledger derivation.

Do not hard-code an incorrect D count.

Emit:

  I_CASES_BLOCKED_REACHABILITY=2
  I_CASES_BLOCKED_CERTIFICATION_CONTRACT=1
  I_CASES_BLOCKED_TOTAL=3

  D_CASES_BLOCKED_REACHABILITY=4
  D_CASES_BLOCKED_CERTIFICATION_CONTRACT=1
  D_CASES_BLOCKED_TOTAL=5

  CASES_BLOCKED_REACHABILITY_TOTAL=6
  CASES_BLOCKED_CERTIFICATION_CONTRACT_TOTAL=2
  CASES_BLOCKED_TOTAL=8

The global MATRIX_OK condition may continue to use the global totals.

Slice labels must not alias global accumulators.

======================================================================
PATCH SCOPE AUTHORIZED
======================================================================

AUTHORIZED PATCH ONLY:

  1. prove D01 synthetic descendant starttime equals captured descendant
     starttime, and any root starttime prerequisite required by exact source;
  2. mechanically bind the D01 DESC_SIGS verifier predicate to exact executor
     bytes;
  3. repair per-slice I/D blocked accounting and preserve global totals;
  4. update evidence labels needed by those corrections.

Retain:

  P5 six predicates + OW_VERDICT gating
  exact PPID cleanup revalidation
  pre-TERM P5 revalidation
  one disclosed direct TERM site
  exact-kill probe gating
  A/I ACCEPTED_PRIOR
  D01 only fresh D execution path
  D02/D03/D04/D06 BLOCKED_REACHABILITY
  D05 BLOCKED_CERTIFICATION_CONTRACT
  no launcher/start_all/SP01/canonical mutation
  RUNNER_EXIT-before-pack

NOT AUTHORIZED:

  D01 host execution
  automatic run
  D02-D06 implementation
  new temporal seam
  SP01 repurposing
  S/C/T work
  full self-lineage work
  launcher/restart
  broadcast
  capital

======================================================================
REQUIRED RETURN
======================================================================

Return revised runner:

  RUNNER_FILENAME
  RUNNER_SHA256
  RUNNER_BYTES
  wc -l
  bash -n

D01 verifier:

  D01_VERIFIER_SOURCE=FIXTURE_DERIVED
  D01_VERIFIER_DESC_STARTTIME_MATCH=YES
  D01_VERIFIER_ROOT_STARTTIME_MATCH=<YES if source-required>
  D01_EXECUTOR_DESC_SIG_DEFINITION_MATCH_COUNT=1
  D01_VERIFIER_DESC_SIG_EXACT_SOURCE_PREDICATE=YES
  D01_VERIFIER_DESC_SIG_MATCH=YES
  D01_VERIFIER_CLOSURE_DERIVED_NOT_CONSTANT=YES
  D01_VERIFIER_USES_EXECUTOR_RUNTIME_G7_G8_FIELDS=NO

Accounting:

  I_CASES_BLOCKED_REACHABILITY=2
  I_CASES_BLOCKED_CERTIFICATION_CONTRACT=1
  I_CASES_BLOCKED_TOTAL=3

  D_CASES_BLOCKED_REACHABILITY=4
  D_CASES_BLOCKED_CERTIFICATION_CONTRACT=1
  D_CASES_BLOCKED_TOTAL=5

  CASES_BLOCKED_REACHABILITY_TOTAL=6
  CASES_BLOCKED_CERTIFICATION_CONTRACT_TOTAL=2
  CASES_BLOCKED_TOTAL=8

Safety remains:

  REAL_KILL_CALL_SITES=1
  D01_EXACT_REVALIDATED_KILL_SITE_COUNT=1
  RUNNER_PATTERN_KILL_SITES=0
  SAFETY_SURFACE fail-closed

After exact-byte Boss review:
  Boss decides whether ONE bounded D01 offline host execution is authorized.

======================================================================
LOCKS
======================================================================

v17 530f40d5...                 STATIC REVIEWED / NOT RUN-AUTHORIZED
A01-A04                         ACCEPTED / CLOSED
I01-I03                         ACCEPTED / CLOSED
I04/I05                         BLOCKED_REACHABILITY
I06                             BLOCKED_CERTIFICATION_CONTRACT

D01                             BUILT / STATIC PATCH REQUIRED
D02                             BLOCKED_REACHABILITY
D03                             BLOCKED_REACHABILITY
D04                             BLOCKED_REACHABILITY
D05                             BLOCKED_CERTIFICATION_CONTRACT
D06                             BLOCKED_REACHABILITY

D host execution                NOT AUTHORIZED
SP01                            NOT AUTHORIZED
new temporal mutation seam      NOT AUTHORIZED
S01-S08                         NOT AUTHORIZED
C01-C08                         NOT AUTHORIZED
T01-T04                         NOT AUTHORIZED
full self-lineage               NOT AUTHORIZED
T05-T10                         EXECUTION-LOCKED
executor 583cfdbc...            NOT CERTIFIED
launcher                        NOT AUTHORIZED
restart                         NOT AUTHORIZED
broadcast                       LOCKED
capital                         UNTOUCHED

CORNELIUS

Nothing to run.
