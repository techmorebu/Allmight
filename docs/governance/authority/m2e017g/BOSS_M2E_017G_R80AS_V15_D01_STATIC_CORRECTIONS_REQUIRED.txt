BOSS M2E-017G R80AS — V15 STATIC REVIEW / D01 CORRECTIONS REQUIRED

TARGET RUNNER REVIEWED

  r80_runner_v15.sh

Exact delivered-file measurements independently verified:

  SHA256     2085cc940c45af10a50796ad5934f4fbc94d7100468131993f269bbd0f82c997
  BYTES      71702
  wc -l      1215
  bash -n    PASS

Target executor remains:

  583cfdbc4dff6a3351136dc98257f19ed1c2735348c6eb553c2ee8e66d50eae7

======================================================================
ACCEPTED STATIC ELEMENTS
======================================================================

Accepted:

  A01-A04 represented as ACCEPTED_PRIOR
  I01-I03 represented as ACCEPTED_PRIOR
  D02-D06 have no executor invocation path
  D02-D06 start no fixture processes

D blocked dispositions remain:

  D02 BLOCKED_REACHABILITY
  D03 BLOCKED_REACHABILITY
  D04 BLOCKED_REACHABILITY
  D05 BLOCKED_CERTIFICATION_CONTRACT
  D06 BLOCKED_REACHABILITY

D01 correctly uses the intended hybrid topology model:

  REAL topology discovery via real /proc parentage
  SYNTHETIC descendant identity via PROC_ROOT

A single direct runner cleanup TERM site is disclosed.

No D host execution is authorized yet.

======================================================================
BLOCKER 1 — "ALL SIX P5" CLAIM CHECKS ONLY FIVE PREDICATES
======================================================================

Current D01 admission loop checks:

  OW_NOT_BASELINE
  OW_NOT_PIDFILE
  OW_NOT_EXECUTOR
  OW_NOT_EXEC_ANCESTOR
  OW_NOT_GOVERNED

It does NOT gate on:

  PID_DESCENDS_FROM_FIXTURE_ROOT

Yet it emits:

  D01_P5_ALL_SIX_PREDICATES_YES

This is incorrect.

Certified P5 remains six predicates:

  PID_DESCENDS_FROM_FIXTURE_ROOT
  PID_NOT_BASELINE_ROOT
  PID_NOT_CANONICAL_PIDFILE_MEMBER
  PID_NOT_EXECUTOR_PID
  PID_NOT_EXECUTOR_ANCESTOR
  PID_NOT_REAL_GOVERNED_RUNTIME

REQUIRED CORRECTION:

For BOTH real D01 fixture PIDs, obtain and gate on the P5 descendant/root
predicate in addition to the existing five exclusions.

If certified P5 exposes that predicate under a specific field name, use that
exact field.

Emit per PID:

  DESCENDS_FROM_FIXTURE_ROOT
  NOT_BASELINE
  NOT_PIDFILE
  NOT_EXECUTOR
  NOT_EXECUTOR_ANCESTOR
  NOT_REAL_GOVERNED_RUNTIME
  P5_VERDICT
  P5_REASON

Final D01 admission must require all six required predicates and certified P5
PASS.

If the parent/root process cannot satisfy the certified descendant predicate
under the chosen fixture-root argument:

  D01 must BLOCK
  do not reinterpret or waive the predicate.

Do not keep the label ALL_SIX unless six predicates are actually checked.

======================================================================
BLOCKER 2 — D01 VERIFIER IS NOT INDEPENDENT
======================================================================

Current D01 executor actual is derived from executor evidence:

  G7_CLOSURE
  G7_FROZEN_DESCENDANTS
  G7/G8 evidence line numbers

Current D01 verifier actual is then constructed from those SAME values.

Therefore:

  executor_actual source = executor TSV
  verifier_actual source = executor TSV

That is duplicated executor evidence.

The fixture-derived I_PREDICTION_BASIS does not fix this because the value
ultimately passed as verifier_actual still contains executor-derived closure,
frozen membership and ordering.

REQUIRED CORRECTION:

D01 independent verifier must derive its prediction/result only from:

  real parent/child process relation
  captured real PIDs
  captured real starttimes
  real non-governed cmdlines
  synthetic PROC_ROOT mirror files
  synthetic descendant signature
  fixture construction order
  static source-grounded executor ordering, if needed as design expectation

It must NOT obtain verifier_actual from:

  G7_CLOSURE
  G7_FROZEN_DESCENDANTS
  G7_FROZEN_COUNT
  G8_CENSUS
  STOP_GATE
  STOP_REASON
  RESULT
  executor TSV line numbers

Recommended separation:

  EXPECTED:
    closure=FROZEN
    frozen_contains_desc=YES
    order=CLOSURE_BEFORE_CENSUS

  EXECUTOR_ACTUAL:
    derived from executor TSV G7/G8 evidence

  VERIFIER_ACTUAL:
    predicted from fixture facts + exact-source control-flow ordering

For example, the independent verifier may establish:

  real pgrep -P should discover D01_RD under D01_RR
  synthetic present(D01_RD)=YES
  synthetic cmdline matches DESC_SIGS
  synthetic starttime matches captured identity
  therefore descendant qualifies for G7 freeze
  exact source places the G7 freeze/emission before G8 census code

This is an independent prediction, not a reread of runtime G7/G8 output.

Then compare executor actual to that prediction.

======================================================================
BLOCKER 3 — CLEANUP IDENTITY REVALIDATION IS TOO WEAK
======================================================================

Current D01 cleanup:

  checks PID numeric
  checks /proc/PID exists
  re-reads cmdline
  requires d01fx marker
  then kill -TERM exact PID

That is better than pattern cleanup, but it is not sufficient identity
revalidation under the standing fixture-signal doctrine.

The captured PID may be reused by a process that happens to retain or mimic a
matching cmdline marker.

REQUIRED CORRECTION:

At fixture creation capture for each real D01 fixture PID:

  PID
  starttime
  cmdline marker
  lineage/parent relation
  ownership/P5 admission identity

Immediately before TERM revalidate at minimum:

  same numeric PID
  same starttime
  expected cmdline marker
  still fixture-owned / lineage-consistent

Only then signal.

If absent:
  ALREADY_ABSENT_NO_SYSCALL

If any identity predicate differs:
  IDENTITY_MISMATCH_NO_SIGNAL

No escalation.
No pattern kill.

======================================================================
BLOCKER 4 — SAFETY SURFACE CONTRADICTS ITSELF
======================================================================

v15 correctly measures:

  REAL_KILL_CALL_SITES=1

but stale v14 lines remain afterward:

  RUNNER_OWNS_NO_DIRECT_SIGNAL_SITE=YES
  RUNNER_KILL_PURPOSE="none; no fixture process is started in this revision"
  terminal display "REAL_KILL_CALL_SITES 0"

Those statements are false in v15.

There is also a duplicate RUNNER_PATTERN_KILL_SITES measurement.

REQUIRED CORRECTION:

Remove stale zero-signal emissions/display.

Retain exactly one authoritative signal-surface declaration:

  REAL_KILL_CALL_SITES=1
  RUNNER_PATTERN_KILL_SITES=0
  RUNNER_OWNS_NO_DIRECT_SIGNAL_SITE=NO
  RUNNER_KILL_PURPOSE=D01 bounded fixture cleanup

The disclosure count and safety gate must derive from the same code-only
observation universe.

======================================================================
PATCH SCOPE AUTHORIZED
======================================================================

AUTHORIZED PATCH ONLY:

  1. enforce all six certified P5 predicates plus certified P5 verdict for both
     D01 real fixture PIDs;
  2. make D01 verifier genuinely independent of executor runtime G7/G8 evidence;
  3. strengthen pre-TERM cleanup revalidation to PID + starttime + cmdline +
     fixture ownership/lineage;
  4. remove stale contradictory zero-signal safety emissions and duplicate
     pattern-kill measurement;
  5. update labels/counters needed for these corrections.

Retain:

  D01 only fresh executable D case
  A/I ACCEPTED_PRIOR scope
  D02-D06 blocked declarations only
  one direct runner TERM site maximum
  no pattern kill
  no launcher/start_all/SP01/canonical mutation
  RUNNER_EXIT-before-pack finalization

NOT AUTHORIZED:

  D host execution
  automatic test run
  new mutation seam
  D02-D06 fixture work
  SP01 repurposing
  S/C/T fixture work
  full self-lineage
  launcher/restart
  broadcast
  capital

======================================================================
REQUIRED RETURN
======================================================================

Return revised runner plus static report:

  RUNNER_FILENAME
  RUNNER_SHA256
  RUNNER_BYTES
  wc -l
  bash -n

D01 P5:

  D01_P5_ROOT_ALL_SIX=YES
  D01_P5_DESC_ALL_SIX=YES
  D01_P5_ROOT_VERDICT=PASS
  D01_P5_DESC_VERDICT=PASS
  D01_P5_WAIVER_USED=NO

Independent verifier:

  D01_VERIFIER_SOURCE=FIXTURE_DERIVED
  D01_VERIFIER_USES_EXECUTOR_G7_FIELDS=NO
  D01_VERIFIER_USES_EXECUTOR_G8_FIELDS=NO
  D01_VERIFIER_USES_EXECUTOR_TERMINAL_FIELDS=NO

Cleanup:

  D01_CLEANUP_STARTTIME_CAPTURED=YES
  D01_CLEANUP_PRETERM_STARTTIME_REVALIDATED=YES
  D01_CLEANUP_PRETERM_CMDLINE_REVALIDATED=YES
  D01_CLEANUP_PRETERM_LINEAGE_REVALIDATED=YES
  D01_CLEANUP_IDENTITY_MISMATCH_NO_SIGNAL=YES

Safety:

  REAL_KILL_CALL_SITES=1
  RUNNER_PATTERN_KILL_SITES=0
  RUNNER_OWNS_NO_DIRECT_SIGNAL_SITE=NO
  RUNNER_KILL_PURPOSE=D01_FIXTURE_CLEANUP
  SIGNAL_DISCLOSURE_EQ_GATE=YES

Scope:

  A execution=0
  I execution=0
  D01 execution path=1
  D02-D06 execution paths=0

After exact-byte Boss review:
  Boss decides whether ONE bounded D01 offline host execution is authorized.

======================================================================
LOCKS
======================================================================

v15 2085cc94...                 STATIC REVIEWED / NOT RUN-AUTHORIZED
A01-A04                         ACCEPTED / CLOSED
I01-I03                         ACCEPTED / CLOSED
I04/I05                         BLOCKED_REACHABILITY
I06                             BLOCKED_CERTIFICATION_CONTRACT

D01                             BUILT / STATIC PATCH REQUIRED
D02                             BLOCKED_REACHABILITY
D03                             BLOCKED_REACHABILITY
D04                             BLOCKED_REACHABILITY
D05                             BLOCKED_CERTIFICATION_CONTRACT
D06                             BLOCKED_REACHABILITY

D host execution                NOT AUTHORIZED
SP01                            NOT AUTHORIZED
new temporal mutation seam      NOT AUTHORIZED
S01-S08                         NOT AUTHORIZED
C01-C08                         NOT AUTHORIZED
T01-T04                         NOT AUTHORIZED
full self-lineage               NOT AUTHORIZED
T05-T10                         EXECUTION-LOCKED
executor 583cfdbc...            NOT CERTIFIED
launcher                        NOT AUTHORIZED
restart                         NOT AUTHORIZED
broadcast                       LOCKED
capital                         UNTOUCHED

CORNELIUS

Nothing to run.
