PHASE 3 APPENDIX — REAL INPUT MCS INTEGRATION
============================================

Status: COMPLETE
Branch: phase3-mcs-real-inputs
Purpose: Replace stubbed Phase-2 MCS inputs with replay-backed, deterministic Phase-3 inputs.

------------------------------------------------------------
1. OBJECTIVE
------------------------------------------------------------
Phase 3 upgrades the Institutional Regime Engine to consume
real, replay-derived Market Confluence Score (MCS) components.

Stub values and config-based defaults are fully removed.
All scores are produced from CSV generators with strict grid
ordering and deterministic semantics.

------------------------------------------------------------
2. PHASE 3 INPUT COMPONENTS
------------------------------------------------------------

The following components are now REQUIRED inputs unless
--allow-missing-components is explicitly provided.

Component               Level   Source
------------------------------------------------
SweepScore              L2      calc_sweep_l2_replay.py
LiquidityArchScore      L3      calc_liquidity_arch_l3_replay.py
MacroScore              L∞      calc_macro_score_replay.py
RiskPenalty             L∞      calc_risk_penalty_replay.py

All components:
- Emit ComponentRow objects
- Produce grid-ordered CSVs
- Are auditable and deterministic

------------------------------------------------------------
3. REGIME ENGINE CHANGES
------------------------------------------------------------

File: scripts/regime/calc_regime_replay.py

Changes:
- MCS inputs are now per-asset maps
- Missing inputs raise hard errors by default
- allow_missing_components gates fallback behavior
- Grid order is authoritative

This enforces Phase 3 correctness at runtime.

------------------------------------------------------------
4. CLI INTEGRATION
------------------------------------------------------------

File: scripts/regime/run_regime_replay.py

New CLI flags:
--sweep-csv
--liquidity-arch-csv
--macro-score-csv
--risk-penalty-csv
--allow-missing-components

These flags inject Phase 3 inputs directly into the regime engine.

------------------------------------------------------------
5. DETERMINISM GUARANTEES
------------------------------------------------------------

Determinism is enforced via:
- Grid-order enforcement
- No random state
- No unordered dict iteration
- Stable sorting of audit output
- Explicit missing-input policy

Validated by:
- test_regime_determinism.py
- test_phase3_activation_band_sensitivity.py

------------------------------------------------------------
6. WAYLAND EXECUTION RULES (CRITICAL)
------------------------------------------------------------

ALL terminal writes MUST follow these rules:

✔ Use:
  python - <<'PY'
  Path(...).write_text(...)

✔ Use:
  wl-paste --no-newline | bash

✖ DO NOT:
  - Paste multi-line Python directly
  - Use heredocs with cat <<EOF
  - Trust clipboard MIME inference

This prevents silent corruption under Wayland.

------------------------------------------------------------
7. CONTRIBUTING RULE (MANDATORY)
------------------------------------------------------------

All future contributors MUST:
- Use Python writer pattern for file creation
- Avoid direct clipboard pastes into editors
- Treat grid order as authoritative everywhere

Violation = invalid build.

------------------------------------------------------------
8. PHASE 3 BUILD STATUS
------------------------------------------------------------

✔ All generators emit ComponentRow
✔ All CSVs consumed by regime engine
✔ All tests passing
✔ Phase 3 determinism locked
✔ Governance codified

PHASE 3 IS CLOSED.
PHASE 4 MAY PROCEED.
