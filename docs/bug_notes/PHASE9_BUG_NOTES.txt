PHASE 9 BUG NOTES
================
As-of: 2025-12-31 08:37 UTC

Resolved during Phase 9
- Broker API shape discovery issues in early tests (fixed by introspecting real signatures).
- Allowlist test initially failed due to declaration container type; corrected to match broker's dict registry.
- NetworkGate http_get_bytes initially inserted with indentation error; repaired with clean rewrite.
- Phase 8 contract regression (get_market_snapshot disabled) fixed by restoring legacy wrapper behavior.
- dummy_toxic redaction path required bypass of network gating in call(); fixed and re-indented.

Open / watch-list
- Keep Phase 8 contract tests as non-negotiable guardrails as Phase 9 expands.
- Ensure future adapters do not introduce SDK threads/hidden retries.
- Ensure allowlist stays capability-scoped (no wildcards, no substring matching).
