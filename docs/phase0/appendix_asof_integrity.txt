PHASE 0 — ASOF INTEGRITY & TEMPORAL TRUTH APPENDIX
=============================================

Definition:
'asof' refers to a strict temporal snapshot (e.g. i60, last).

Rules:
- All replay components MUST be generated explicitly per asof
- No implicit fallback from last → i60 is permitted
- Missing component artifacts are a hard error

Guarantees:
- Phase-4 control logic consumes only committed replay artifacts
- Confidence, band transitions, and promotion gates are time-consistent
- Historical truth is preserved

Implementation:
- build_components_asof.py is the only approved generator
- python -m module execution is mandatory
- filenames must include asof suffix

This appendix exists to prevent temporal leakage.
