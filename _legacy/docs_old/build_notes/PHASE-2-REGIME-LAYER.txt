PHASE 2 — Institutional Regime Layer (Replay-Relative)

Status: ACTIVE (Regime v1 implemented; Excel-faithful Tab 40–45 constants pending extraction)

Implemented:
- scripts/regime/regime_types.py
  - InstitutionalRegimeState, RegimeAssetContribution
  - Contribution includes audit tags: bias_source, asset_role
- scripts/regime/calc_regime_replay.py
  - Deterministic regime classifier v1 (breadth + confirmation)
  - No lookahead, no smoothing
  - Audit fields emitted:
    - driver_sum
    - risk_driver_sum / safe_driver_sum
    - risk_breadth / risk_breadth_neg
    - confidence
- scripts/regime/run_regime_replay.py
  - Reads replay-relative GRID artifacts from outputs/replay/
  - Enforces grid ordering deterministically (file order must match --grid)
  - Field mapping:
    - Bias: L0.structure_bias if not NEUTRAL, else fallback to L0.trend_simple
    - StructureScore: L0.ssp_total_structure_score
    - PressureScore: L1.pressure_score
  - Adds per-asset audit tags:
    - bias_source: L0.structure_bias | L0.trend_simple_fallback
    - asset_role: RISK | SAFE (SAFE: XAU/PAXG)
  - Emits:
    - outputs/replay/regime_state_GRID_*_{last|i60}.json (newline-terminated)

Tests:
- tests/conftest.py (ensures repo-root imports for pytest)
- tests/regime/test_regime_determinism.py

Validation:
- last vs i60 sensitivity confirmed (GRID BTC,ETH,XRP,XAU 15m):
  - last: TRANSITION (mixed breadth; SAFE confirmation not enough to flip)
  - i60: RISK_OFF (risk_breadth_neg = 1.0 and SAFE confirmation)
