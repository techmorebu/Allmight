PHASE 12 BUILD NOTES
============================================================
As-of: 2026-01-02 10:18 UTC

Goal
----
Multi-source, read-only market snapshot orchestration via AdapterBroker.market_snapshot_multi().

What changed
------------
- Implemented robust wrapper unwrapping + quote normalization to canonical MarketSnapshot.
- Ensured broker routes reads through AdapterBroker.call(operation="market_snapshot_live").
- Added deterministic adapter ordering in audit output.
- Produced audit fields expected by tests:
  - pair
  - merge_policy
  - inputs_used_count
  - results (per-adapter statuses)
  - merge_audit (if available)

Key invariants preserved
------------------------
- No retries, no writes, no trust expansion.
- Deterministic behavior for adapter list ordering.
- Strictly read-only orchestration suitable for later live-arming controls.

Verification
------------
- pytest -q
  -> 112 passed

Files touched
-------------
- allmight/adapters/broker.py
