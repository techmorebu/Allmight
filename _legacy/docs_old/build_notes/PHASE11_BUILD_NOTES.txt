PHASE 11 BUILD NOTES
====================

As-of: 2026-01-02 06:12 UTC
Phase: 11
Status: COMPLETE (ready to freeze once wrap is committed)

0) GOAL (PHASE 11)
------------------
Add *read-only* snapshot reconciliation primitives that do NOT expand trust:
- Pure merge policies (deterministic, test-defined behavior)
- Optional normalization helpers (pair enforcement + invalid filtering)
- Optional audit-friendly aggregation output
- Small reusable “band/spread” helper used by stricter merge logic
- Keep invariants: no credentials, no retries, no new network allowlists, no execution.

1) WHAT CHANGED (HIGH LEVEL)
----------------------------
A) Pure merge policies (core)
- allmight/adapters/snapshot_merge.py
  - merge_snapshots(...): merge policies:
    - pick_first            (deterministic “first wins”)
    - pick_first_valid      (first *valid* wins; explicit refusal when none)
    - median                (median price; even counts average the two center values)
    - reject_outliers       (drop outliers around a center reference; keep center mass)
    - median_strict         (requires min_sources + max_spread_pct; explicit refusal codes)

B) Normalization + merge helper
- allmight/adapters/snapshot_normalize_merge.py
  - normalize_and_merge(...): enforce pair consistency and filter invalids before merging

C) Audit-oriented aggregate result
- allmight/adapters/snapshot_aggregate.py
  - SnapshotAggregate: holds merged snapshot + audit metadata (inputs, kept/rejected, policy)

D) Shared spread/band helper (micro-refactor)
- allmight/adapters/snapshot_band.py
  - small pure helper(s) for computing spread/band checks used by strict reconciliation rules
  - median_strict refactored to reuse this helper (behavior unchanged)

E) Rapid scan phase-awareness overlay
- tools/rapid_scan.py
  - rewritten cleanly after a patch-corruption incident
  - now prints Phase 11 “authoritative files to read” section (5.5) when present

2) TESTS ADDED / UPDATED
------------------------
New Phase 11 test modules:
- tests/phase11/test_phase11_snapshot_merge.py
  - covers all merge policies + explicit refusal codes
- tests/phase11/test_phase11_snapshot_normalize_merge.py
  - pair enforcement + invalid filtering + merge behavior
- tests/phase11/test_phase11_snapshot_aggregate.py
  - aggregate output structure + audit expectations
- tests/phase11/test_phase11_snapshot_band.py
  - band/spread helper correctness

3) INVARIANTS CHECK
-------------------
- No network changes / no allowlist expansion
- No credential handling
- No retries/backoff/schedulers
- No write/execution paths added
- Behavior is test-defined with explicit refusal strings for strict modes

4) HOW TO VERIFY (OPERATOR QUICKCHECK)
--------------------------------------
# sanity
python -m py_compile allmight/adapters/snapshot_merge.py   allmight/adapters/snapshot_normalize_merge.py   allmight/adapters/snapshot_aggregate.py   allmight/adapters/snapshot_band.py   tools/rapid_scan.py

# tests
pytest -q

# project scan
python tools/rapid_scan.py

Expected: clean git, green tests, and Phase 11 overlay section present.

5) OUTPUT ARTIFACTS (PHASE WRAP)
--------------------------------
This phase must end with committed:
- docs/build_notes/PHASE11_BUILD_NOTES.txt
- docs/bug_notes/PHASE11_BUG_NOTES.txt
- docs/phase11/PHASE11_OPERATOR_NOTES.txt
- docs/phase11/PHASE11_HANDOFF_PROMPT_PHASE12.txt

END PHASE 11 BUILD NOTES
