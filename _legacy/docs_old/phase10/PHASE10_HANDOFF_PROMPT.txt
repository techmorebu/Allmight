PHASE 10 HANDOFF PROMPT
=========================
As-of: 2026-01-02 00:34 UTC
Status: ACTIVE
Predecessor: Phase 9 (Allowlist-gated public HTTP snapshot adapter)


0. NON-NEGOTIABLE ENTRY PROCEDURE
--------------------------------
Before any Phase 10 work begins, the following MUST be completed:

1) Read:
   - docs/appendices/APPX_CHAT_RESPONSE_PROTOCOL.txt
   - docs/appendices/APPX_PROJECT_RAPID_SCAN_PROTOCOL.txt

2) Run:
   python tools/rapid_scan.py

If this step is skipped, all subsequent work is invalid.


1. RAPID ORIENTATION (REQUIRED)
-------------------------------
Current Phase: 10

Phase 9 delivered:
- Default-deny network model
- Capability-gated HTTP snapshot adapter
- Enforced allowlists
- Phase 8 redaction contracts preserved
- 100% passing test suite

Phase 10 begins from a STABLE, LOCKED baseline.


2. PHASE 10 SCOPE (INITIAL)
---------------------------
Phase 10 is concerned with:

- Extending live-read adapters WITHOUT expanding trust
- Maintaining deny-first invariants
- Preserving test-first enforcement
- No retries, no credentials, no write operations

Any feature that weakens these invariants is out-of-scope.


3. HARD INVARIANTS (CANNOT BE BROKEN)
------------------------------------
- Network is default-deny
- All live operations route through AdapterBroker.call
- Allowlists are explicit and capability-scoped
- Tests define behavior, not comments
- Redaction is never bypassed for real adapters
- Legacy helpers remain safe stubs


4. FORBIDDEN ACTIONS
-------------------
- No silent network access
- No wildcard allowlists
- No credential introduction
- No bypassing NetworkGate except for explicit test-only adapters
- No modification of Phase 8/9 contracts without versioned appendices


5. REQUIRED OUTPUTS FOR PHASE 10
--------------------------------
Phase 10 MUST conclude with:
- Build notes
- Bug notes
- Operator notes
- Phase 11 handoff
- Updated appendices if behavior changes


6. META
-------
If a future operator asks:
"What am I allowed to do?"

Then this handoff or the appendices have failed.


END OF PHASE 10 HANDOFF
