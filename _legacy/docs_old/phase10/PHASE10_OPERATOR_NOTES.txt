PHASE 10 OPERATOR NOTES
=======================

As-of: 2026-01-02 01:19 UTC
Status: AUTHORITATIVE

What Phase 10 adds
------------------
- A second read-only live snapshot adapter:
  - adapter_id: phase10_http_snapshot_kraken
  - domain: api.kraken.com (must be explicitly allowlisted)
- A shared MarketSnapshot struct used by both Coinbase + Kraken adapters.

How operators should run live reads (policy discipline)
-------------------------------------------------------
- Live reads MUST be invoked via Phase 7 CLI pathways that call:
  AdapterBroker.call(operation="market_snapshot_live")

- Operators must supply:
  - adapter_id (explicit)
  - domain (must match allowlist exactly)
  - pair (symbol string)

What operators must NOT do
--------------------------
- Do NOT add wildcard domains (e.g., *.kraken.com) to allowlists.
- Do NOT add additional domains without tests proving deny-first behavior.
- Do NOT introduce keys/secrets/auth headers.
- Do NOT add retries/backoff or background scheduling.

Verification routine
--------------------
- git status
- pytest -q

Expected: clean + 83 passed

If you see failures
-------------------
- If failing with DENY_NOT_ALLOWLISTED_DOMAIN:
  - confirm config/phase9/network_allowlist_v0.json includes exact (adapter_id, capability, domain)
- If failing with DENY_NETWORK_DISABLED:
  - confirm NetworkGate(enabled=True) is only used where explicitly intended for tests/operator runs.

End of operator notes.
