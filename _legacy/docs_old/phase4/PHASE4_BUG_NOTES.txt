PHASE 4 — BUG NOTES

Closed issues (fixed during Phase-4 bringup):
- Missing PyYAML dependency caused pytest collection failure
- weights.yaml sum-to-one strict validation failure (rounding)
- execution_matrix missing activation_band entries (KeyError)
- Multiple syntax errors from unsafe patching attempts
- Output schema drift: missing inputs.global_confidence required by tests
- i60 component CSVs absent (runner failed on missing files)
- scripts import failures when running component scripts as standalone (fixed via python -m)

Remaining risks / watch-items:
- Band flips can be frequent; promotion gate must remain strict
- Confidence thresholds must remain explicit and test-backed
- If new activation_band labels appear, execution_matrix coverage test must catch it

Freeze rule:
- Do not “quick patch” Phase-4 while frozen. Use UNFREEZE note + tests-first.
